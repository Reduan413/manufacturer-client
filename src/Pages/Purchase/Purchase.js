import React from 'react';
import AvailableProducts from './AvailableProducts';

const Purchase = () => {
    return (
        <div>
        <AvailableProducts/>
        </div>
    );
};

export default Purchase;